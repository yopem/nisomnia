---
import type { HTMLAttributes } from "astro/types"
import { cn } from "@yopem-ui/utils"

type Props = HTMLAttributes<"ul">

const { class: className, ...attrs } = Astro.props
---

<ul class={cn("flex flex-row items-center gap-1", className)} {...attrs}>
  <slot />
</ul>
